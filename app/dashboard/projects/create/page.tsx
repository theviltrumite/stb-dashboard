import CreateProjectForm from '@/app/ui/dashboard/create-project-form';

export default function CreateProjectPage() {
    return (
        <main className="min-h-[calc(100vh-6rem)] bg-gray-50 py-10 px-4">
            <CreateProjectForm />
        </main>
    );
}
